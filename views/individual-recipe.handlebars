<section>
    <h3>{{recipe.name}}</h3>
    <a href= "../profile/{{recipe.user.id}}">
        <p>By: {{recipe.user.name}}</p>
    </a>
    <p>{{recipe.ingredients}}</p>
    <p>{{recipe.instructions}}</p>
</section>